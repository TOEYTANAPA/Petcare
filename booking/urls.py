#-*- coding: utf-8 -*-
from django.conf.urls import patterns, url

from . import views


# urlpatterns = patterns('',
#     url(r'^$', views.IndexView(), name='b'),
#     url(r'^time/$', booking.views.getTime, name='day'),

# )
